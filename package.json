{
  "name": "fixed-size-executor",
  "version": "0.1.1",
  "description": "Submit async jobs to an executor, but limit how many can run concurrently",
  "main": "executor.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "node_modules/mocha/bin/mocha"
  },
  "author": "Rachel Evans",
  "license": "Apache 2.0",
  "devDependencies": {
    "jshint": "^2.8.0",
    "mocha": "^2.2.5",
    "mocha-jshint": "0.0.9",
    "should": "^6.0.3"
  },
  "dependencies": {
  }
}
